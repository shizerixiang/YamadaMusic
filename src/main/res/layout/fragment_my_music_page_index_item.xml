<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="10dp"
    android:paddingBottom="14dp">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="5dp"
        android:layout_marginStart="20dp"
        android:contentDescription="@string/my_music_index_item_image"
        android:id="@+id/my_music_index_item_image"
        android:src="@drawable/main_index_local_music_icon"
        android:maxHeight="37dp"
        android:maxWidth="32dp"
        android:adjustViewBounds="true"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="9dp"
        android:layout_marginStart="16dp"
        android:id="@+id/my_music_index_item_text"
        android:textColor="#707070"
        android:text="@string/app_name"
        android:textSize="18sp"/>

</LinearLayout>